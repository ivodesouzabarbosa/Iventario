using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IventarioController : MonoBehaviour
{

    public List<GridControl> _gridControl;

    // Start is called before the first frame update
    void Start()
    {
      //  gridList[0].SetSiblingIndex(gridList.Count);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
